import {APP} from '../constants';

export const containerWidth = APP.WIDTH;
export const sliderWidth = 16;
export const contentWidth = containerWidth - sliderWidth;

export const sliderOffset = sliderWidth / 2;
